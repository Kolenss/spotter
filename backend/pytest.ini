[pytest]
; config.settings_test is config.settings on SQLite. See that module for why
; the database cannot be swapped from a fixture.
DJANGO_SETTINGS_MODULE = config.settings_test
python_files = test_*.py
testpaths = hos routing trips
filterwarnings =
    ignore::DeprecationWarning
