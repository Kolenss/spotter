# Same origin in production: vercel.json rewrites /api/* to the Django service,
# so the browser never crosses an origin and CORS never enters into it. The dev
# default in api.ts stays http://localhost:8000/api.
VITE_API_BASE_URL=/api
